<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.macula.cloud.admin.mapper.SRMCompanyMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        insert into sys_company
        (
        business_id, name,
        source, code,
        is_deleted, is_enable,
        create_by, create_time,
        update_by, update_time
        )
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.businessId}, #{item.name},
            #{item.source}, #{item.code},
            #{item.isDeleted},#{item.isEnable},
            #{item.createBy},now(),
            #{item.updateBy}, now()
            )
        </foreach>
    </insert>

    <update id="updateBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" open="" close=";" separator=";" >
            update sys_company
            <set>
                business_id=#{item.businessId},
                name=#{item.name},
                legal_person=#{item.legalPerson},
                business_license=#{item.businessLicense},
                update_time=now()
            </set>
            where business_id = #{item.businessId}
        </foreach>
    </update>

    <delete id="deleteBatch" parameterType="java.util.List">
        delete from sys_company where business_id in
        <foreach collection="list" item="item" index="index" open="( " close=" )" separator=",">
            #{item.businessId}
        </foreach>
    </delete>

</mapper>
